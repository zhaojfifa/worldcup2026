# MVP-2 — LLM Narrative Provider Review (DeepSeek vs Gemini)

## ★ Re-run 2026-06-11 — Product Proof sprint (3 samples × zh/vi, v2 product contract)

> Basis: 12 real outputs (zero mock) in `docs/data_audit/mvp2_product_proof_narratives/`
> (855737 upset recap · 979139 final recap · 2026_brazil_argentina pre-match), all 12 passing
> `check_mvp2_product_narrative_guard.py`. This re-run answers the 2026-06-10 verdict: with the
> prediction-first prompt + v2 contract, **neither provider reads as post-match journalism anymore.**

| Dimension (Owner list) | DeepSeek (`deepseek-chat`) | Gemini (`gemini-2.5-flash`) |
|---|---|---|
| 更适合运营 (ops copy) | **强** — operator_copy 短、带数字、可直接发群（「369分Elo差距翻车」） | 偏长（vi 至 447 字符）、群文案有推销腔（“洞察先机！”“独家观点等你”） |
| 更适合结构化证据 | 好（source_ref_map 完整） | **强** — 因子结构稳、subscription_hook 分层最清楚 |
| 更适合中文 | **强** — 球迷语感（翻车/冷门密码/硬解） | 清晰但偏正式，偶现「超神/逆天」式媒体腔 |
| 更适合越南语 | **强** — 自然、短句、「cơn lốc Mbappé」级标题 | 自然但 Title-Case 媒体标题腔（“Khi Bản Lĩnh Lấn Át Mọi Rủi Ro”） |
| 更少 AI 味 | **强**（一次 retry 即收敛） | 需更多 retry（AI-filler tone、溯源缺失各被 guard 拦截一次以上） |
| 更能写订阅/入群动机 | 钩子更准（免费 vs 完整层级 + 临场 30 分钟更新点名） | 话术更全但更像广告位 |
| 风险因子覆盖密度 | 5–6 条/样例 | 3–4 条/样例 |

**结论（维持并强化）：默认 provider = DeepSeek；Gemini = benchmark/对照。** 前端三页绑定 DeepSeek；
Gemini 产物保留供对照。**两家初稿都写过 vi 盘口黑话（kèo/cửa trên/cửa dưới）和（DeepSeek 一次）编造
t.me 链接 —— guard 不是保险层而是必经层**；generator 已把完整 guard 接进 retry loop。
工程参数注记：DeepSeek vi 需 `max_tokens≥4500`（3000 截断）；Gemini 需 `maxOutputTokens≥4600` + `thinkingBudget=0`。

---
（以下为 2026-06-10 v1 评审存档 — 其「敘事像赛后新闻」的 FAIL 已由本轮 v2 prompt/contract 修复。）

> **Date:** 2026-06-10 · **Subject:** real DeepSeek (`deepseek-chat`) vs Gemini (`gemini-2.5-flash`) narratives
> for 855737, zh-CN + vi-VN, generated by `scripts/mvp2_generate_scoutscore_narrative.py` and **all four passing**
> `scripts/check_mvp2_llm_narrative_guard.py`. Artifacts: `docs/data_audit/mvp2_llm_narratives/855737.*.{deepseek,gemini}.json`.
> Both are **real LLM output** (`llm_provider` = deepseek / gemini), not mock.
>
> **★ Owner verdict (2026-06-10):** the provider recommendation below stands (DeepSeek default, Gemini benchmark),
> but the **current narrative product-angle FAILED** — both providers' output reads like post-match journalism, not a
> Giành Cup AI ScoutScore model judgement. The fix is **prompt + contract + guard** (provider-agnostic), in the next
> sprint **MVP-2 Betting-Logic Model Narrative Prompt Revision** (prediction first-principles — pre-match judgement →
> risk factors → validation → next-match watch; NOT gambling). **Re-run this review after the revision.**

Note: the **raw** first-round output of BOTH providers failed the guard (leaked factor keys like `team_strength`,
drifted schema `signal`/`detail`, dropped fields in vi). After tightening the prompt + naturalizing the input
(engineering's job — schema/prompts/guards), the **regenerated** output of both providers passed. The comparison
below is on the guard-passing version.

---

## Side-by-side
| Dimension | DeepSeek (`deepseek-chat`) | Gemini (`gemini-2.5-flash`) |
|---|---|---|
| 更像产品文案 (product-copy feel) | **强** — 标题「纸面碾压，效率翻车」punchy，像体育编辑 | 好，但偏正式/略长（「强队迷思」是亮点） |
| 更懂足球用户语言 | **强** — 「翻车」「冷门密码」等球迷语感 | 标准、清晰，球迷语感稍弱 |
| 更适合中文运营 (operator copy) | **强** — operator_copy 一句话、可截图、不啰嗦 | 偏长、偏「官方」，CTA 略带推销味（「洞察赛场风云！」） |
| 越南语自然度 | 自然、简洁 | 自然、更详细（基本持平，各有所长） |
| 更少工程味 | **强**（收紧后）| 干净（收紧后），source_ref_map 偏冗余（输入因子名 + 信号名都塞进去） |
| 更会保留证据边界 | 好 | **更全** — 多带 formation/lineups 来源；更明确把 xG/injuries 标为「下一场要看」 |
| 篇幅 / 密度 | 紧凑 | 更长、信息更全 |

## Recommendation
- **默认 provider：DeepSeek。** 产品文案 / 运营文案的「人话感」「可截图感」更强，hero 与 operator_copy 更适合越南/缅甸私域运营。
- **Benchmark / 备选：Gemini。** 当需要更完整的证据/边界铺陈（如带阵型来源、更细的 takeaway）时更合适，可作为对照或回退。
- **排除：Kimi**（既往比较：vi/mm 漏中文）。
- 两者都通过 guard；最终 provider 由 Owner 拍板。本轮前端**绑定 DeepSeek** 作为 `/recap` + `/evidence` 的叙事来源，
  Gemini 产物保留在 `docs/data_audit/mvp2_llm_narratives/` 供对照。

## Caveats
- 单场样本（855737），temperature 0.5；不同次生成会有措辞差异——这是叙事模型的预期行为，guard 负责守住边界。
- 这是历史回放样例的**赛后**叙事；非赛前预测、非命中宣称（`internal_notes` 保留 historical-replay/MISS 表述）。
- mock 仅在 provider 不可用 / guard 失败时作为**已标记**回退（`llm_provider=mock`），不是默认路径。
