# 1489371 Match-Day Lifecycle Validation — evidence log

Fixture: 1489371 Brazil vs Morocco · kickoff 2026-06-13 22:00 UTC · main @ e695c08
Owner runtime instruction 2026-06-13. **No fabrication** — LIVE/FT rows are filled by the
operator at the real times; the engineering rows below are real or clearly-labelled logic proofs.

## 1. BEFORE KICKOFF — real run @ 2026-06-13T02:27Z（T-1173min）

```
status-refresh : 1489371 SCHEDULED  today=True  recap_pkg=False  (api_football)
                 1489369 RECAP_READY today=False recap_pkg=True   (2-0)
                 1539000 RECAP_PENDING today=False recap_pkg=True
  -> docs/data_audit/mvp2_daily_refresh/fixture_lifecycle_20260613_0227.json
check_fixture_freshness : FRESHNESS PASS (3 fixtures, 2 known WARN on 1539000)
package today 1489371 zh : ALLOWED (exit 0, primary_scoreline 2-1, lifecycle_gate=allowed)
```
Verdict (before kickoff): **PASS** — pre-match packaging allowed as expected.

## 2. LIVE / FINISHED / RECAP — deterministic logic proof（SIMULATION, not live-API）

Pure `decide()` / `gates()` over kickoff 22:00 UTC — proves the gate the operator will see tonight:

| scenario | state | today_pkg | recap_pkg |
|---|---|---|---|
| 22:50 api=2H | LIVE | False | False |
| 22:10 api stale NS | LIVE (kickoff-passed freeze) | False | False |
| 00:20 api=FT | FINISHED | False | True |
| 02:00 api=FT no recap | RECAP_PENDING | False | True |
| 02:00 recap bundled | RECAP_READY | False | True |

CLI behaviour while LIVE/finished (proven in P1.2 round + re-proven here): `package today` REFUSED,
`refresh` prints `NO_VALID_TODAY_FIXTURE` and overwrites stale today/next `.md` with a REFUSED stub.

## 3. Scanner behaviour when 1489371 is finished-class — SIMULATION（1489371 forced LIVE）

`check_fixture_freshness.py` → **FRESHNESS FAIL, 10 stale surfaces (exit 1)**:
- bundled narrative mode zh/vi/my = `pre_match_2026_modeling` → /predict + /share/fixture stale (×3)
- HomePage hero/rescore hook still pins 1489371 as 今日主推 (×1)
- stale today/next package files still carry pre-match copy (×6) — **self-clear on `refresh`**

**All TRUE POSITIVES.** The package-file ones clear when the operator re-runs `refresh` for EACH
language (zh/vi/my). The narrative-mode + hero-pin ones are **deploy-bound** — they clear only
after the A4 recap is bundled (mode=real_recap) and the frontend is redeployed with an updated
hero. This is the **P1.2b** follow-up (frontend freshness), not a gate failure.

## 4. TONIGHT — operator fills these rows（per runsheet §6）

```
[ ] LIVE  @ ~22:30Z : status-refresh / freshness / package today / refresh --lang zh|vi|my
       expect: LIVE · today refused · NO_VALID_TODAY_FIXTURE · stale pkgs stubbed · scanner FAIL (hero pin) = TRUE POSITIVE
[ ] FT    @ ~00:20Z : status-refresh / freshness
       expect: FINISHED · today refused · recap pkg still refused (no recap narrative yet)
[ ] FT+45 @ ~00:45Z : A4 recap (mvp2_ops.py recap) -> bundle -> status-refresh / refresh / package recap
       expect: RECAP_READY · recap pkg ALLOWED · recap uses recap projection
[ ] after frontend redeploy with recap + updated hero : freshness -> PASS
```

## 5. Recommendation (engineering, pre-LIVE)

Expected end-state tonight = **PASS WITH ISSUES**: the lifecycle GATE works (no finished/live
fixture can be emitted as a today pre-match package — the BLOCKED case is prevented), but the
FRONTEND surfaces (bundled narrative mode + HomePage hero pin) are deploy-bound and the scanner
will TRUE-POSITIVE FAIL until the A4 recap is bundled and the frontend redeployed — that is the
**P1.2b** scope (auto-rotate hero off a finished fixture / show recap-pending on stale /predict).
Final verdict is the operator's after the real LIVE/FT runs.
